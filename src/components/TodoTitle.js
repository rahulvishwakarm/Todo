import React from 'react'

const TodoTitle = (props) => {
    return (
        <div>
            <h1>
                {props.title}
            </h1>
        </div>
    )
}

export default TodoTitle;
